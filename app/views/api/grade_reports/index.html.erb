<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 20px;
    }

    th, td {
      border: 1px solid #ddd;
      padding: 8px;
      text-align: left;
    }

    th {
      background-color: #f2f2f2;
    }

    .odd-row {
      background-color: #f9f9f9;
    }

    .even-row {
      background-color: #ffffff;
    }
  </style>
</head>
<body>

  <table>
    <tr>
      <th>Description</th>
      <th>Count</th>
    </tr>
    <% @grade_report.each_with_index do |report, index| %>
      <tr <%= index.odd? ? 'class="odd-row"' : 'class="even-row"' %>>
        <td><%= report['Description'] %></td>
        <td><%= report['Count'] %></td>
      </tr>
    <% end %>
  </table>

</body>
</html>
